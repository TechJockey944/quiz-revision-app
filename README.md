# quiz-revision-app

This repository contains a simple study website in the `frontend` folder.

Open `frontend/index.html` in a browser to use the app. It lets you paste a prompt or upload text files, then builds a personalized learning pathway, essay, flashcards, and quiz.